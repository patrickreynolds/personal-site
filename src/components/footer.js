// import { Link } from "gatsby"
import PropTypes from "prop-types"
import React from "react"

const TempFooter = () => (<div></div>)

const Header = ({ siteTitle }) => (
  <footer
    style={{
        backgroundColor: `lightgray`,
        height: `80px`,
      }}>
    <p
      style={{
        textAlign: `center`,
      }}>
      Footer of Patrick's Site
    </p>
  </footer>
)

export default TempFooter